name: lazy_asset_generator_example
description: Example project for lazy_asset_generator.
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  lazy_asset_generator:
    path: ..

dev_dependencies:
  build_runner: ^2.4.13

flutter:
  assets:
    - assets/images/
    - assets/data/
